% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_DNA.R
\name{filter_variant_ScIGMA}
\alias{filter_variant_ScIGMA}
\title{--------------------------------------------------------------- #
Filter variants and cells in a ScIGMA_object (R6)}
\usage{
filter_variant_ScIGMA(
  obj,
  min.dp = 10,
  min.gq = 30,
  vaf.ref = 5,
  vaf.hom = 95,
  vaf.het = 35,
  min.cell.pt = 50,
  min.mut.cell.pt = 1
)
}
\arguments{
\item{obj}{A `ScIGMA_object` (R6) with the following fields:
- `meta.data`
- `cell.ids`, `cell.labels`
- `variants`, `variant.filter`
- `vaf.mtx`, `gt.mtx`, `dp.mtx`, `gq.mtx`
- `amps`, `amp.normalize.method`, `amp.mtx`
- `proteins`, `protein.normalize.method`, `protein.mtx`}

\item{min.dp}{Integer. Minimum sequencing depth (DP) per cell/variant to be considered.
Default: `10`.}

\item{min.gq}{Integer. Minimum genotype quality (GQ) threshold.
Default: `30`.}

\item{vaf.ref}{Numeric. Maximum tolerated VAF for reference genotypes (GT = 0).
Default: `5`.}

\item{vaf.hom}{Numeric. Minimum expected VAF for homozygous alternate genotypes (GT = 2).
Default: `95`.}

\item{vaf.het}{Numeric. Minimum expected VAF for heterozygous genotypes (GT = 1).
Default: `35`.}

\item{min.cell.pt}{Numeric. Minimum percentage of cells required to cover a variant.
Default: `50`.}

\item{min.mut.cell.pt}{Numeric. Minimum percentage of cells required to be mutated
(GT = 1 or 2) for a variant to be retained.
Default: `1`.}
}
\value{
A new `ScIGMA_object` (clone) containing only the filtered variants and cells.
  The original object is not modified.
}
\description{
This function applies quality-based filtering on variants and cells stored
in a `ScIGMA_object` (R6). Filtering is performed based on minimum depth (DP),
genotype quality (GQ), and VAF/GT consistency. Variants are retained only if
they are sufficiently covered across cells and observed in a minimum fraction
of mutated cells. Cells are retained only if they contain a sufficient number
of evaluable variants. The function returns a **filtered clone** of the input
object, leaving the original object unchanged.
}
\examples{
\dontrun{
filtered_obj <- filter_variant_ScIGMA(my_scigma_obj,
                                      min.dp = 15,
                                      min.gq = 40,
                                      vaf.ref = 3,
                                      vaf.hom = 90,
                                      vaf.het = 30,
                                      min.cell.pt = 60,
                                      min.mut.cell.pt = 5)
}

}
\seealso{
- `DelayedMatrixStats` for efficient matrix operations when using DelayedArray objects.
- The original `filter_variant` function from the optima package for conceptual inspiration.
}
