% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_H5Processing.R
\name{loadH5_dir_HDF5}
\alias{loadH5_dir_HDF5}
\title{Load and merge multiple HDF5 files from a directory (HDF5-backed)}
\usage{
loadH5_dir_HDF5(
  dir,
  pattern = "\\\\.h5$",
  recursive = FALSE,
  omic.type = c("DNA+protein", "DNA"),
  block.size = 1e+08,
  feature_policy = c("identical", "intersect"),
  verbose = TRUE
)
}
\arguments{
\item{dir}{Character. Directory containing `.h5` files.}

\item{pattern}{Character. Regex to match file names (default: `"\\.h5$"`).}

\item{recursive}{Logical. Search subdirectories (default: `FALSE`).}

\item{omic.type}{`"DNA+protein"` or `"DNA"`. Passed to `loadH5_HDF5()`.}

\item{block.size}{Numeric. DelayedArray block size in bytes (default `1e8`).}

\item{feature_policy}{Character. `"identical"` (require same features/order across files)
or `"intersect"` (keep only common features; reorder consistently). Default `"identical"`.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
A \code{ScIGMA_object} containing the merged HDF5-backed matrices.
}
\description{
Reads all `.h5` files from a directory, converts each into an
HDF5-backed `ScIGMA_object`, and **merges them by cells** using `rbind`.
Features (columns) are aligned beforehand via \code{\link{harmonize_cols}}
(policy: `"identical"` or `"intersect"`).
}
\examples{
\donttest{
obj <- loadH5_dir_HDF5("data/h5/", omic.type = "DNA+protein",
                       feature_policy = "intersect")
obj$print()
}
}
