% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_DNA.R, R/run_compass.R
\name{run_compass_mcmc}
\alias{run_compass_mcmc}
\title{Run COMPASS MCMC phylogeny inference}
\usage{
run_compass_mcmc(
  variant_matrices,
  locus_regions,
  region_matrix,
  output_prefix = NULL,
  locus_names,
  locus_chromosomes,
  region_names,
  region_chromosomes,
  cell_names = NULL,
  chains = 4L,
  chain_length = 5000L,
  patient_sex = "female",
  use_cna = TRUE
)

run_compass_mcmc(
  variant_matrices,
  locus_regions,
  region_matrix,
  output_prefix = NULL,
  locus_names,
  locus_chromosomes,
  region_names,
  region_chromosomes,
  cell_names = NULL,
  chains = 4L,
  chain_length = 5000L,
  patient_sex = "female",
  use_cna = TRUE
)
}
\arguments{
\item{variant_matrices}{List. Contains 'REF', 'ALT', and 'GT' matrices.}

\item{locus_regions}{Integer vector. Maps each locus to a region index (0-based).}

\item{region_matrix}{Integer matrix. Region counts (regions x cells). Required.}

\item{output_prefix}{Character. Prefix for output tree and data.}

\item{chains}{Integer. Number of MCMC chains.}

\item{chain_length}{Integer. Iterations per chain.}

\item{patient_sex}{Character. "female" or "male".}

\item{input_dir}{Character. Path to matrices directory.}

\item{output_dir}{Character. Path for COMPASS output.}

\item{compass_exec}{Character. Path or alias to COMPASS executable.}

\item{n_iters}{Integer. Number of MCMC iterations per chain.}

\item{restarts}{Integer. Number of random initializations.}

\item{threads}{Integer. Cores allocated for parallel chains.}
}
\value{
Logical. TRUE if successful.

Logical TRUE on success.
}
\description{
Triggers the COMPASS CLI via system call. Isolates MCMC memory overhead
from the main R process.

Direct interface to the ETHZ C++ COMPASS backend. Runs natively in R memory.
This function strictly requires Copy Number Alteration (CNA) data.
}
